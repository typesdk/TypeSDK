<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle"
>
    <!-- 实心   -->
    <solid android:color="#ffffffff"/>
    <!-- 描边  -->
     <stroke
           android:width="5dp"
           android:color="@color/wd_color_blue" />
    <stroke
            android:width="3dp"
            android:color="@color/wd_color_blue" />
    <!-- 圆角 -->
    <corners
            android:radius="10dp" />
    <padding
            android:left="1dp"
            android:top="1dp"
            android:right="1dp"
            android:bottom="1dp" />

</shape>